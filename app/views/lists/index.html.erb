<div class="container">
  <div class="h1-index-list">
    <h1 class="liste-des-films">Liste des films</h1>
  </div>

  <div class="row d-flex justify-content-center">
    <% @lists.each do |list| %>
      <div class="col-sm-12 col-md-3">
        <%= list.name %>
          <%= cl_image_tag list.photo.key, class: "d-block w-100" %>
      </div>
    <% end %>
  </div>
</div>
