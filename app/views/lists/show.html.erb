<div class="container">
  <div class="h1-show-list">
    <h1 class="show-du-films">Voici les films </h1>
  </div>

   <div class="row d-flex justify-content-center">
      <div class="col-sm-12 col-md-3">
        <%= @list.name %>
          <%= cl_image_tag @list.photo.key, class: "d-block w-100" %>
      </div>
  </div>

    <div class="row d-flex justify-content-center">
      <div class="bookmarks-all">
          <% @list.bookmarks.each do |bookmark| %>
            <%= bookmark.comment %>
            <%= link_to "Supprimer",
              bookmark_path(bookmark),
              method: :delete,
              data: { confirm: "Are you sure?"},
              class:"btn btn-dark" %>
          <% end %>
      </div>
    </div>
    <div class="row d-flex justify-content-center">
      <div class="aller-sur-la-creation-d1-avis">
        <%= link_to "Rédigez un avis",
           new_list_bookmark_path(@list),
           class:"btn btn-dark"
         %>
      </div>
    </div>
</div>
